# onnxruntime C API headers

Verbatim copies of the ONNX Runtime C API headers needed to build the plugin
execution provider (`include/onnxruntime/core/session/` upstream). They define
`ORT_API_VERSION`; any `onnxruntime` requirement must be at least this release.
Sync the headers and the requirement together on ORT upgrades, and keep the files
unformatted.

Last synced from:

* https://github.com/microsoft/onnxruntime.git
* tag: v1.30.0
* commit: f2c39fe2f838cf35ce7da92824f5a5e3ee6e88a7
* date: 2026-09-10
