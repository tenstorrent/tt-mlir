[pytest]
# Skip the benchmarks directory during recursive collection - `pytest tests/python`
# runs the sanity suite. An explicit path (`pytest tests/python/benchmarks/...`)
# overrides this, since pytest always collects from paths passed on the CLI.
norecursedirs = benchmarks
