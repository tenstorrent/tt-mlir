# CI artifact downloads (3.5 GB) -- re-fetchable with fetch.sh
raw/
# symlink views onto raw/, rebuilt by link_graphs.sh
graphs
models
# profiler working files: ttrt artifact dirs, flatbuffers, raw tracy reports, logs.
# The measurement record that analysis needs is fleet/*.matmuls.csv and fleet/*.percore.csv,
# which are committed; ops_perf_results.csv is only needed to re-run rederive.sh.
fleet/*.art/
fleet/*.ttnn
fleet/*.ops_perf_results.csv
fleet/*.stdout
fleet/*.translate.err
*.log
__pycache__/
# regenerated by assemble.sh from the committed sources
device_tables.md
guard_tables.md
report_tables.md
