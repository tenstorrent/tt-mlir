--extra-index-url https://download.pytorch.org/whl/cpu
torch@https://download.pytorch.org/whl/cpu/torch-2.9.1%2Bcpu-cp312-cp312-manylinux_2_28_x86_64.whl; sys_platform == "linux"
torch==2.9.1; sys_platform == "darwin"
# NOTE: Must stay >=, not pinned. LLVM/MLIR python requirements install nanobind
# (currently 2.12.0 via nanobind>=2.9,<3.0). Pinning here causes a mid-build
# downgrade that races with the python_runner compilation in the explorer build of tt-xla.
nanobind>=2.10.2
